couchdb:
  client:
    url: http://127.0.0.1:25984/
    username: nccu
    password: nccu
    query-strategy: view
    schema-operation: create
    initialization-strategy: single

zookeeper:
  connectString: "localhost:32181"
  maxRetries: 3
  baseSleepTimeMs: 1000

application:
  id: data-sender-test